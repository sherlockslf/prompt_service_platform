spring:
  application:
    name: psu-platform
  threads:
    virtual:
      enabled: true
  
  # 数据库配置 生产环境
  # datasource:
  #   driver-class-name: com.mysql.cj.jdbc.Driver
  #   url: jdbc:mysql://rm-bp1v0aoc564umyqbp.mysql.rds.aliyuncs.com:3306/aim_product_ai?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai
  #   username: aim_user
  #   password: Aim@2026

  # 开发配置atC
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/psu?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: a123456
    
  # JPA配置
  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
        use_sql_comments: true
        
  # Redis配置
  data:
    redis:
      database: 0
      port: 6379
      host: r-bp1pol95oaq920vgio.redis.rds.aliyuncs.com
      password: Aim@2026
      timeout: 5000
      lettuce:
        pool:
          max-active: 8
          max-idle: 8
          min-idle: 0
          max-wait: -1ms

# 大模型API配置
llm:
  provider: qwen
  api-key: sk-78c98ba04206418babc522196aef5712,sk-1281bcb7d31e4113b4691fe8d7969dcc,sk-dc201fb8e55748dda0da7d14d68b84c0
  base-url: https://dashscope.aliyuncs.com/api/v1
  model: qwen-max
  timeout: 30000
  max-retries: 3

# 安全配置
security:
  jwt-secret: 387a9f2c4e7b1d0569802aefc7d45081b3ad7ef27c98641db5a9c23e8701f4d6
  encryption-key: 9s7k2p5r8z1q4w7e3t6u9c0v2b5n8m1l

# 服务器配置
server:
  port: 8084

# 日志配置
logging:
  level:
    com.example.psu: INFO
    org.springframework.security: WARN
    org.hibernate.orm.connections.pooling: WARN
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"

# Actuator配置（健康检查等）
management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always
